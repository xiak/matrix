# Codex working checkpoint

> Non-authoritative portable memory. Validate Git, exact CI and owning FEAT.

- Updated 2026-09-16. Repository https://github.com/xiak/matrix.git,
  branch feat/iam; only this independent worktree is writable for this task.
- Latest verified production rollback point:
  40407e2710a45ee1000552146cd362740074369a, readonly product discovery.
  Verification34959581661 exact SHA checked through GitHub API:
  go, authority-process, node-process all completed/success.
- Prior family runtime f15cc983a69092528a66eb49b0509b760392187c /
  Verification34955695756 all3 success. Its source/performance, frozen
  compilation, SQL admission and historical proof evidence belong to005.
  Checkpoints and acceptance-only commits are not implementation donors.
- Actual source remains IAM23/Audit13/PaaS1; published installation profile
  deliberately unchanged. No release admission or binary N-1 claim.

## Goal and next owner

Whole IAM goal ACTIVE. Previous turn made concrete progress: implemented,
verified, committed and pushed CAT-06. No blocker or repeated no-progress state.
Read AGENTS then owning FEAT/code/tests; do not scan all docs.
001 owns Profile/current registry/discovery;005 policy/conditions/delegation;
006 Role/Trust/STS;008 product/ABAC;010 peer UI;011 final HA/capacity/release.
Next bounded work: converge006 Role/Trust/STS into real vertical slices, first
freeze exact management/trust/session and actor/history contracts with consumers.
005 IP conditions, Role boundary and safe delegation also remain unfinished.
Do not treat completed directory or User boundary as whole IAM acceptance.

No subagents or extra tasks. UI exclusively the UX/UI peer. Do not read/copy
other worktrees' WIP or change their environments. Withdrawn GitLab/root1.5
task excluded; no remote1.3/.160/.161 work, remote or shared service restarts.
User authorized stopped local containers and unused custom networks cleanup;
previous bulk cleanup already completed, images/volumes retained. Do not
repeat broad cleanup as a progress substitute.

## Fixed CAT-06

GET /v1/authorization-profiles has no query/body/cursor/history/tenant selector.
Effective USER bearer binds current Account; existing iam.policy.list checks
the real ACCOUNT INSTANCE via original serializable management/PDP/boundary.
No new Action, SQL shape, schema number, registration or publication authority.
Platform-only/service/verifier/forced-change credentials do not borrow access.

AuthorizationProfileList{apiVersion,kind,accountId,items:[{profile,contentDigest}]}.
Full source-equal registered declarations, product ascending/unique,1–16items,
whole JSON <=64KiB. Current synthetic account-catalog response5products/28861B.
Account binds read context, not product ownership. INSTALLATION/PROBE metadata
remains complete to preserve original digest; CUSTOMER editor must label/disable
it. Decoder/Go validate digest and semantics; JSONSchema alone is not authority.
Shared canonical encoding unchanged. No permit, cached Allow, head selection,
truncation or constant fallback. Complete registry/source check and locks precede
return; uncertainty503 rolls back normal decision/outbox.

Existing integration owner proves both real accounts, whole registered bytes,
no-role/list grant/publication refusal, ordinary Deny/boundary/revoke/retirement/
logout, rejected selectors and drift503 without partial decisions/facts.
Final policy119.31s(directory3.60s,registry3.31s),HTTP90.77s,recovery19.04s;
IAM package232.562s, PG18.6 1CPU/768MiB/PIDs128/maxconn64;
Go2/768MiB,race-p1 serial. Original101members/120s flow/240s parent unchanged.
Final independent dualIAM/Audit/PaaS+dispatchers54.76s/package57.486s:
real discovery -> compile -> HTTP publish same commitment -> explicit attachment
-> PaaS Allow/Deny/revoke; actual restricted database logins, dual-tenant
resources/configuration/Operation/outbox/history preserved. Replica DB disconnect
returns503/no metadata, reconnect and IAM restart retain exact catalog/account.
Not DB failover or whole HA. No browser/old-binary opt-in claimed locally;
independent CI retains its existing fixed-predecessor/Audit gates.
Full Go race/vet/modules/API generation byte stability/Linux build passed.
Own test clients exited; exact labelled PG container/network removed, synthetic
volume retained. No own fixture remains running, no peer UI or remote touched.

## Preserved family/current/historical contract

Author patterns are TENANT-only product.family.*: exactly three segments and
one nonempty complete final declaration segment. Profile declarations and
actual requests stay exact. Full frozen Profile validation; no scope/condition/
resource filtering.128resolved/statement,8192visits before dedup,64KiBauthor,
128KiBcompilation,16Profiles. Original exact commitments unchanged.

Publish accepts only author document plus original intent/revision; source
registry locked and SQL independently expands complete sets. Trusted archive
loader recompiles; a rehashed transport-valid missing/invented member is not
authority. Single PDP checks whole frozen/current compatibility before
Effect/resource/conditions, then only exact resolved SID actions. No runtime
wildcard/author fallback/current-head expansion of old immutable versions.
Explicit new publication AND default choice; creating a policy never attaches it.

Current private resource_kind_for_action/is_platform_action use registered
current projections (STABLE invoker,owner-only,PARALLEL UNSAFE), no second CASE
catalog. Missing scope/kind closes; duplicate match errors. Exact ABI/ACL/
search_path/readiness preserved. Historical facts use original immutable archive,
never current authorization. Create10/claim7/recorder7/evidence5,
ServiceIdentity/lookup_service and Audit canonical unchanged.

PolicyVersion1 forbids compilation;2 requires full commitment. Current
attachment/boundary evidence binds version+compilation; private {value,canonical}
storage never escapes. Fixed1dc is the single explicit interpretation
predecessor, not unpublished schema1..N or marker provenance. Original rows,
defaults, revoked attachments and chain bytes never rewritten/revived; genuine
legacy CUSTOMER requires explicit supported preflight/republish path in005.
Future-source r2 gate adds synthetic inspect AND removes a condition; old family
does not expand, incompatible nonmatching Deny fails503, revoke restoresAllow.
Not a real PaaS inspect endpoint or signed release proof.

## Peer handoff

UX/UI工程师:01a07b21-9a0d-7fd0-b090-7827ce18262e, own
feat/cloud-console-ux. User explicitly wants proactive ready-slice notifications.
40407e27 exact CI success and complete CAT-06 contract sent2026-09-16;
peer may consume fixed backend without waiting for Role/STS.
Its latest report: first f15 real browser attempt made no boundary writes and
was stopped/not accepted. Combining only its export with old static adapter
omitted __next files. Peer owns full static host+export/query navigation fix
and new independent fixture. Never implement or import that WIP here.
Account/User/Group clients exist; Policy versions and User boundary require
peer live wiring; full editor/Role/STS must not be represented as accepted live.
Existing opt-in TestIAMConsoleBrowser in test/authorityprocess supplies real
services on isolated random loopback, bounded30minutes, finish marker checks
actual setA/replaceB/remove facts+chain. Needs its own complete UI build and
separate visual evidence, not merely PASS. Peer's fixed4317 service is not ours.

Phase3:01a04149-5dbb-7300-9e4c-31d9e85c8ada receives only fixed objects.
40407e27 all3 success sent; still waits ONE final cumulative IAM/Role/STS donor,
retaining its PaaS5/host and predecessor5/4/5+r11. No early schema adoption.
Final host donor be3c4a96b4381426c01cd6315eaa3713c2855982 includes complete
platform12+ca7 enrollment. Do not import peer checkpoint or acceptance state.
Public IAM/Audit owner window remains ours; send exact Role/actor/proof surface
before new shared contract changes. Do not alter PaaS/node/installation.

Programmatic credentials, Role/Trust/STS, service roles/ABAC, security governance,
complete peer UI, capacity/HA and final signed release remain open.
Pre-v1 converged schema becomes first supported release; digits never prove
SQL ABI or cross-profile compatibility.
